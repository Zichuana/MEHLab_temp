# Experimental base photos

Photos referenced from `data/bases.json`, for example:

```
img/bases/example-1.jpg
img/bases/example-2.jpg
```

One base can list many images. Frames are fixed size; images use
`object-fit: cover` and stay centered regardless of original dimensions.
